import React from 'react'

export const metadata = {
  title: "The Football Hub | Teams | Squad",
};

const SquadPage = ({ params }) => {
  const { id } = params;

  return (
    <div>SquadPage</div>
  )
}

export default SquadPage